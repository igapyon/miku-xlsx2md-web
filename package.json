{
  "name": "miku-xlsx2md-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "refresh:runtime": "node scripts/refresh-miku-xlsx2md-runtime.mjs",
    "build": "node scripts/build-miku-xlsx2md-web.mjs",
    "test": "vitest run",
    "test:unit": "vitest run",
    "build:all": "npm run build && npm run test:unit"
  },
  "devDependencies": {
    "jsdom": "^26.0.0",
    "typescript": "^5.7.3",
    "vitest": "^3.0.0"
  }
}
